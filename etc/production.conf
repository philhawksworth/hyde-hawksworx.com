<VirtualHost *:80>

    ServerName hawksworx.com
    ServerAlias www.hawksworx.com
    ServerAdmin phawksworth@gmail.com
    
    # RewriteEngine on
    # RewriteCond %{HTTP_HOST}   ^hawksworx\.com/journal/ [NC,OR]
    # RewriteCond %{HTTP_HOST}   !^$
    # RewriteRule ^/(.*)         hawksworx.com/blog/$1 [L,R=permanent]

    <Directory /var/www/hawksworx.com/>
        Options MultiViews
        Order deny,allow
         Allow from all
    </Directory>

    DocumentRoot /var/www/hawksworx.com/
    
</VirtualHost>